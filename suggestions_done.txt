# Applied Changes Ledger
# Format: FILE	LOCATOR	BEFORE_SNIPPET	AFTER_SNIPPET	NOTES

.gitignore	filename (new file)	N/A (file did not exist)	Created comprehensive .gitignore for Python, MODELLER outputs (models/, with_hetero/, *.pir, *.ali, etc.)	Added to prevent committing generated files and intermediate artifacts
Yazdani_7068679_assignment5_1.py	filename	Yazdani_7068679_assignment5_1.py	model_comparison.py	Renamed to remove matriculation number and assignment reference for professional presentation
model_comparison.py	line 1	import os.path	import os\nimport os.path\nfrom pathlib import Path	Added pathlib import for better cross-platform path handling
model_comparison.py	lines 98-106	if os.path.exists("./models"):\n    print("models directory already exists")\nelse:\n    os.mkdir("./models")\nlog.verbose()\nenv = Environ()\nenv.io.output_directory = './models'	models_dir = Path("./models")\nif models_dir.exists():\n    print("models directory already exists")\nelse:\n    models_dir.mkdir(parents=True)\nlog.verbose()\nenv = Environ()\nenv.io.output_directory = str(models_dir)	Updated to use pathlib for directory creation, more robust and cross-platform
model_comparison.py	lines 122-125	if not os.path.exists("./with_hetero"):\n    os.mkdir("./with_hetero")\nos.chdir("./with_hetero")	hetero_dir = Path("./with_hetero")\nif not hetero_dir.exists():\n    hetero_dir.mkdir(parents=True)\nos.chdir(str(hetero_dir))	Updated to use pathlib for directory creation in hetero block
model_comparison.py	lines 225-232	if os.path.exists("./models"):\n    print("models directory already exists")\nelse:\n    os.mkdir("./models")\nlog.verbose()\nenv = Environ()\nenv.io.output_directory = './models'\nenv.io.hetatm = True	models_dir = Path("./models")\nif models_dir.exists():\n    print("models directory already exists")\nelse:\n    models_dir.mkdir(parents=True)\nlog.verbose()\nenv = Environ()\nenv.io.output_directory = str(models_dir)\nenv.io.hetatm = True	Updated second models directory creation to use pathlib
requirements.txt	entire file	biopython>=1.79\nnumpy>=1.21.0	# Core dependencies\nbiopython>=1.79\nnumpy>=1.21.0\nrequests>=2.25.0\n\n# Note: MODELLER requires special installation from Sali Lab\n# Visit: https://salilab.org/modeller/\n# MODELLER is freely available for academic use with registration\n# After installation, obtain a license key and configure it	Added requests dependency and documented MODELLER special installation requirements
README.md	entire file	Old assignment-focused README with University references, old folder names (raya00001_7068679-assignment5), old script name (Yazdani_7068679_assignment5_1.py), assignment language	New portfolio-grade README with professional title, comprehensive sections (Overview, Problem & Approach, Tech Stack, Setup, How to Run, Data & Inputs, Outputs, Reproducibility, Troubleshooting, License, Citation), aligned with project_identity.md	Complete rewrite to remove all assignment traces and present as professional portfolio project
